# 🎓 미니 원서 앱 — 세미나 실습 가이드 (처음부터 끝까지)

> 터미널 없이 **브라우저만으로** 내 원서 앱을 만들고, **클로드에게 말해서 바꾸는** 실습입니다.
> 아래를 **위에서부터 순서대로** 하나씩 따라 하세요. 한 단계 끝날 때마다 ✅ 체크.

---

## 0단계 · 준비물 (세미나 전에 미리)

| 준비물 | 어디서 | 비고 |
|---|---|---|
| GitHub 계정 | github.com | 코드 저장소 |
| Supabase 계정 | supabase.com | 데이터베이스 (무료) |
| Vercel 계정 | vercel.com | 배포 (무료) — **GitHub으로 로그인** 추천 |
| Claude 유료 구독 + Claude Code | claude.ai / code.claude.com | "말해서 바꾸기"용 |
| **Anthropic API 키** | console.anthropic.com | **AI 문제 생성용** (각자 발급). 2차 세미나 때 받았으면 그거 재사용 |

> 💡 **팁**: 🔑 **꼭 미리 만들 건 GitHub 하나뿐.** Vercel·Supabase는 수업 중 각 사이트에서 **"Continue with GitHub"** 을 누르면 그 자리에서 바로 가입돼요(미리 안 만들어도 OK).
> 🔑 **Anthropic API 키가 없다면** → **[교재.md](교재.md)의 「🔑 Anthropic API 키 발급받기」** 참고 (카드 안 될 때 크롬 언어 영어로 바꾸는 팁 포함).

---

## 1단계 · 코드 가져오기 (강사 코드 → 내 저장소, Use this template)

1. 강사가 알려준 GitHub 주소를 엽니다 → **`github.com/nauta325/mini-reader`**
2. 오른쪽 위 초록색 **`Use this template`** → **`Create a new repository`**
3. 저장소 이름 정하고(예: `my-mini-reader`) → **`Create repository`** → **내 계정에 깨끗한 복사본**이 생깁니다
   - 주소가 `github.com/(내아이디)/…` 로 바뀌면 성공 ✅

> 📌 Use this template = 강사 코드를 **커밋 기록 없이 깨끗한 내 저장소로 복사**하는 것. 이제부터 이건 **내 저장소**예요.
>
> ⚠️ **GitHub 첫 화면 당황 방지** — 로그인하면 "Home · Ask anything · Create project…" 같은 화면이 떠요. **여기선 아무것도 누르지 마세요.** 위 저장소 주소(또는 QR)로 **바로 이동해서 Use this template**만 누르면 됩니다. (마지막 `Create repository`는 눌러도 돼요 — 홈의 `Create project`와는 달라요.)

---

## 2단계 · 데이터베이스 만들기 (Supabase)

### 2-1. 새 프로젝트 생성
1. [supabase.com](https://supabase.com) → **`Continue with GitHub`** 로그인 → **`New project`**
   - 처음이면 **`Authorize Supabase`** → `Authorize` 누르기(안전) · **`Create a new organization`** 뜨면 **Type=`Personal`, Plan=`Free`**, 이름 아무거나
2. 이름 아무거나 (예: `my-reader`), **Region은 `Northeast Asia (Seoul)`**, DB 비밀번호는 정해서 **메모**
3. **`Create new project`** → 1~2분 기다리면 준비 완료

> ⚠️ **꼭 새 프로젝트로.** 기존에 쓰던 프로젝트에 섞으면 나중에 헷갈립니다.

### 2-2. 표(테이블) 만들기
1. 왼쪽 메뉴 **`SQL Editor`** → **`New query`**
2. 내 저장소의 **`sql/mini-setup.sql`** 파일을 열어 **전체 복사**
   - (GitHub에서 파일 열고 **`Raw`** 버튼 누르면 통째로 복사 잘 됨)
3. SQL Editor에 붙여넣기 → 오른쪽 **`Run`** ▶
4. **`Success`** 뜨면 표·저장공간이 한 번에 생성됨 ✅

### 2-3. (선택) 데모 데이터 넣기 — 화면이 바로 채워져요
- 같은 방식으로 **`sql/mini-seed-demo.sql`** 도 복사 → Run
- 학생 6명·책 4권·점수까지 들어가서, 배포하자마자 대시보드가 꽉 차 보여요
- 실제로 쓸 땐 지우면 됩니다 (파일 맨 위에 지우는 SQL 있음)

### 2-4. 주소·키 복사 (다음 단계에서 씀)
1. 왼쪽 **`Settings`(⚙️) → `API`**
2. **`Project URL`** 복사 → 메모 (`https://xxxx.supabase.co`)
3. **`service_role`** (secret) 키 복사 → 메모
   - ⚠️ **`anon`/`publishable` 말고 `service_role`(secret)** 이어야 함
   - ⚠️ 이 키는 **비밀번호 같은 것.** 남한테 보여주거나 코드에 넣지 말 것

---

## 3단계 · 배포하기 (Vercel) — ⭐가장 중요한 함정 예방⭐

> **환경변수를 "Deploy 누르기 전에" 넣으세요.** 배포 후에 넣으면 다시 배포해야 반영돼서 헷갈립니다.

1. [vercel.com](https://vercel.com) → **`Continue with GitHub`** 로그인 → **`Add New…` → `Project`**
   - **`Secure Your Account`(2FA)** 뜨면 **`Skip securing my account`** 눌러 넘어가기(필수 아님)
2. GitHub 저장소 목록에서 **내 `mini-reader`** 옆 **`Import`**
   - 목록이 비었으면 **`Install`(GitHub 앱 설치)** → 내 계정 선택. (오른쪽 **`Clone Template`** 카드는 무시, **왼쪽 목록에서 내 저장소 Import**)
3. **Deploy 누르기 전에**, 아래로 내려 **`Environment Variables`** 펼치기 → 2개 추가:

   | Name (이름) | Value (값) | 필수? |
   |---|---|---|
   | `SUPABASE_URL` | 2-4에서 복사한 **Project URL** | ✅ 필수 |
   | `SUPABASE_SERVICE_KEY` | 2-4에서 복사한 **service_role 키** | ✅ 필수 |
   | `ANTHROPIC_API_KEY` | 본인 Anthropic 키 | 선택 · AI 문제 생성 |
   | `NOTION_TOKEN` | 노션 통합 토큰(`ntn_…`) | 선택 · 노션에서 학생 가져오기 |

   - **AI 자동생성용** `ANTHROPIC_API_KEY` = 본인 Anthropic 키 (없으면 [교재.md](교재.md)의 「🔑 API 키 발급받기」 먼저)
   - **노션에서 학생 가져오기** 쓰면 `NOTION_TOKEN`도 추가 (아래 **부록 · 노션에서 학생 가져오기** 참고). 둘 다 선택이라 나중에 넣고 Redeploy 해도 됩니다.
4. 이제 **`Deploy`** 클릭 → 1~2분 후 **`Congratulations`** 🎉
   - `Install Coding Agent Plugin` 같은 카드가 떠도 **무시하고 ⊗로 닫기**
5. **`Continue to Dashboard`** → 상단 **`Visit`** 버튼으로 사이트 열기

> 🔑 모든 키는 **Vercel 환경변수(서버)에만.** HTML/브라우저 코드엔 절대 안 넣음 (학생·학부모가 소스를 볼 수 있어서).

---

## 4단계 · 접속 확인

- 내 사이트 주소는 **`https://(내프로젝트).vercel.app`** 예요.
- ⚠️ 앱은 **`/mini/`** 아래에 있어요. 주소 끝에 **`/mini/`** 를 붙여서 여세요:
  ```
  https://(내프로젝트).vercel.app/mini/
  ```
  (루트로 들어가도 자동으로 `/mini/`로 넘어갑니다.)
- **빨간 책 로고 홈 화면**이 뜨면 성공! ✅ 이 홈은 **학생용**이에요:
  - 학생: 가운데 큰 **학생 시작하기** 버튼
  - 선생님: 맨 아래 작은 **🔑 선생님용 관리 페이지** 링크 → `/mini/admin.html`

### 🔒 선생님/학생 화면 분리 (학생이 관리 화면 못 보게)
- 학생 홈에는 **관리 메뉴가 안 보여요.** 관리(학생·배정·원서)는 **선생님용 페이지(`/mini/admin.html`)** 로 따로 들어갑니다.
- 처음엔 비밀번호가 없어 누구나 들어갈 수 있어요 → 선생님용 페이지에서 **🔐 관리자 비밀번호 설정**을 한 번 해두면, 그 뒤로는 **비밀번호를 알아야** 관리 화면이 열립니다.
  - 학생이 주소를 알아도 관리 페이지를 열면 **비밀번호 창**이 떠서 막혀요.
  - 비밀번호는 앱 안에서 정하고, **코드·주소 어디에도 안 보이게** 서버에만 저장돼요.

### 로그인·데모 (데모 데이터를 넣었다면)
- 학생 화면 로그인: 아이디 `seojun` + 이름 `김서준` (그 외 haeun/doyun/jiwoo/sia/minjun)

---

## 4½단계 · 🧪 다 같이 공통 테스트 (똑같이 해서 잘 되는지 확인)

> **여기까지는 모두 같은 앱**이에요. **다 같이 똑같은 순서로** 해보면 강사가 한눈에 확인해요. (데모 데이터 넣었다고 가정)

**👦 학생으로** (홈 → 학생 시작하기)
1. `seojun` / `김서준` 로그인
2. 책 열기 → **단어 학습** 넘겨보기
3. **소리내어 읽고 녹음** → 🎤 시작 → 읽고 → ⏹ 멈추기 → **"✅ 녹음 저장됐어요"**
4. **원서퀴즈 1** 풀고 제출 → 점수 화면

**🔑 선생님으로** (홈 맨 아래 🔑 선생님용)
5. (처음이면) **관리자 비밀번호 설정**
6. **원서 배정·결과 → 결과 대시보드**
7. 김서준 **퀴즈 점수** 보이는지 · **▶ 녹음 재생** 되는지
8. **월별 추이 그래프 · 학생별 현황 표** 보이는지

✅ 8개 다 되면 성공! · 🆘 녹음 실패 → `recordings` 버킷(전체 SQL 다시 Run) / 데이터 없음 → `SUPABASE_URL`·SQL 확인

### 📋 이 앱으로 되는 것 · ➕ 더 해볼 것
- **선생님**: 학생·반 등록(일괄·노션) · 책/퀴즈 **AI 생성** · 유튜브 지문 · 배정 · 대시보드(점수분포·**월별추이**·**현황표**·녹음) · 리포트/워크시트 인쇄
- **학생**: 배정 책(**제목만** 보이는 목록 → 눌러서 단어·녹음·퀴즈 메뉴) · 읽어주기 영상 · 단어카드 · **녹음** · 원서퀴즈 · 끝낸 단계 **완료 ✓**(미통과는 **다시 풀기**) 표시
- **➕ 추가 아이디어(말만 하면)**: "포인트·스티커", "주간 랭킹", "틀린 단어 복습 퀴즈", "학부모용 리포트" …

---

## 5단계 · 내 학원에 맞게 바꾸기 (바이브 코딩 — Claude Code)

1. 크롬에서 **code.claude.com** → **새 세션** (처음이면 GitHub 승인)
2. 내 저장소(`(내아이디)/mini-reader`)를 선택해 연결
3. 아래처럼 **말로** 시켜보기 (하나씩):

| 이렇게 말하면 | 이렇게 바뀝니다 |
|---|---|
| "대표 색을 분홍색으로 바꿔줘" | 앱 전체 색(`--brand`) |
| "통과 점수를 80점으로 바꿔줘" | 퀴즈 통과 기준 |
| "리포트 학원 이름을 '○○영어'로 바꿔줘" | 리포트/워크시트 학원명 |
| "레벨 목록을 우리 학원 레벨로 바꿔줘" | 레벨 드롭다운 |
| "홈 화면에 우리 학원 로고를 넣어줘" | 홈 화면 |

4. 클로드가 고치고 **저장(push)** 하면 → **Vercel이 다시 배포** → 새로고침하면 반영

### 💡 함께 해보기 — "화면 나누기"도 말로 됩니다 (수업 실습 추천)
한 화면에 여러 기능이 몰려 있으면 **탭으로 나눠 달라**고 말하면 돼요. 강사가 시범 → 참가자 따라하기 좋은 실습입니다.

| 이렇게 말하면 | 이렇게 바뀝니다 |
|---|---|
| "원서 배정·결과를 **탭으로 나눠**줘" | 배정 / 대시보드 두 탭 (이미 되어 있음 — 순서·기본 탭만 바꿔보기) |
| "**결과 대시보드를 기본 탭**으로 해줘" | 처음 열 때 대시보드부터 |
| "학생 화면도 **책 읽기 / 내 기록**으로 나눠줘" | 학생 화면 탭 분리 |
| "탭 이름을 우리 학원 말투로 바꿔줘" | 탭 라벨 |

> 👉 **핵심 메시지**: "이렇게 나눠줘 / 저렇게 합쳐줘" 처럼 **화면 구성도 말로** 바꿉니다. 정답이 없으니 참가자마다 다르게 나와도 OK.

---

## ❓ 자주 막히는 곳 (트러블슈팅)

| 증상 | 원인 · 해결 |
|---|---|
| 루트 주소에서 **404** | 앱은 `/mini/` 에 있음 → 주소 끝에 `/mini/` 붙이기 |
| 화면은 뜨는데 **`undefined/rest/v1…` 오류** | `SUPABASE_URL` 이 비었거나 오타 → Vercel 환경변수 확인 후 **Redeploy** |
| **`column … does not exist`** | `sql/mini-setup.sql` 을 **끝까지** 실행 안 함 → 전체 다시 Run |
| **`PGRST204 … schema cache`** | 표는 맞는데 캐시가 옛것 → Supabase **Settings→General→Restart project** |
| 코드 고쳤는데 **사이트 그대로** | ① Vercel에서 **Redeploy** 했는지 ② **강력 새로고침**(Ctrl/Cmd+Shift+R) ③ 보고 있는 주소가 **내 프로젝트 주소** 맞는지 |
| 학생 수·데이터가 이상함 | 지금 앱이 보는 DB = Vercel `SUPABASE_URL` 이 가리키는 그 Supabase. 여러 프로젝트 있으면 **어느 걸 가리키는지** 확인 |
| **service_role vs anon** | 로그인·저장이 안 되면 키가 `anon`일 수 있음 → **`service_role`(secret)** 으로 교체 후 Redeploy |

---

## 📎 부록 · AI로 책 만들기 (지문 넣으면 퀴즈 자동 생성)

> 지문(원서 본문)만 있으면 **단어카드 + 원서퀴즈 1·2**를 AI가 자동으로 만들어 줘요. (⚠️ `ANTHROPIC_API_KEY` 필요)

1. **🔑 선생님용 관리 페이지 → 원서 관리 → `🤖 AI로 책 만들기 (지문 붙여넣기)`**
2. **책 제목** 입력 · **시리즈·레벨** 선택 · (챕터북이면 **챕터 번호**도, 그림책은 비움)
3. **영어 지문** 붙여넣기 (또는 아래 유튜브 스크립트로 자동) · (선택) 읽어주기 **영상 URL**
4. **`🤖 AI로 생성 & 저장`** → *"✅ 완성! 단어 N · 원서퀴즈1 N · 원서퀴즈2 N"* 뜨면 성공 → 학생에게 **배정**
- 💡 챕터북은 **챕터마다 따로**(생성 후 챕터 자동 +1) · 출제 언어(자동/영어/한글) 선택 가능
- 🆘 **원서퀴즈1이 0개**면 지문이 너무 짧은 경우 → 지문을 더 넣고 다시 생성 (그림책은 퀴즈가 하나만 나올 수 있음)

## 📎 부록 · 유튜브 스크립트로 지문 자동 채우기 (선택)
1. 원서 관리 화면 → **"🎬 유튜브 스크립트를 지문으로 자동 가져오기"** 펼치기
2. **[📎 유튜브 스크립트 담기]** 를 브라우저 **북마크바로 드래그** (한 번만)
3. 유튜브 영상에서 **더보기 → 스크립트 표시** → 그 북마크 클릭 → **새 탭**으로 책 만들기 화면이 열리며 제목·지문이 자동으로 채워짐 (시리즈·레벨만 고르고 **AI로 생성 & 저장**)

---

## 📎 부록 · 노션에서 학생 가져오기 (선택 — 노션으로 학생 관리하는 분만)

> 학생 명단을 노션 데이터베이스로 관리한다면, 하나씩 입력하지 않고 **통째로 가져올 수** 있어요.
> ⚙️ **처음 한 번만** 아래 A(연결 설정)를 하면, 그다음부터는 B(붙여넣기)만 하면 됩니다.

### A. 한 번만 설정 — 노션 통합 토큰 연결
1. **[notion.so/my-integrations](https://www.notion.so/my-integrations)** → **`New integration`** → 이름(예: `mini-reader`) → 만들기
2. **`Internal Integration Secret`** (`ntn_…` 또는 `secret_…`) **복사**
3. Vercel → 내 프로젝트 → **`Settings → Environment Variables`** → **`NOTION_TOKEN`** = 그 시크릿 추가 → **Redeploy**
   - ⚠️ 이 토큰도 **비밀값** — 코드/화면에 넣지 말고 Vercel에만.
4. 노션에서 **학생 데이터베이스(표)** 열기 → 우측 위 **`···`(더보기) → `Connections`(연결) → `+ Add connections` → 방금 만든 `mini-reader`** 선택
   - (이걸 안 하면 "권한 없음/못 찾음"이 나요. 통합이 그 표를 볼 수 있게 **공유**하는 단계예요.)

### B. 가져오기 (평소엔 이것만)
5. 학생 관리 → **`☁ 노션에서 가져오기`** 펼치기
6. 노션 학생 DB **링크(또는 ID)** 붙여넣기 → **`미리보기`**로 확인 → **`가져오기`**

**표 형식**: 열 이름이 **이름 · 아이디 · 전화 · 레벨 · 반** 이면 자동으로 알아봐요. **제목(Title) 열 = 이름**.

🆘 **"NOTION_TOKEN 환경변수가 없어요"** → A-3(Vercel에 `NOTION_TOKEN` 넣고 Redeploy)을 안 한 거예요.
🆘 **"권한 없음 / 못 찾음"** → A-4(노션 표를 통합에 Connections로 공유)를 안 한 거예요.

---

**끝!** 여기까지 하면 나만의 원서 앱이 인터넷에 살아 있고, 이제 클로드에게 말해서 계속 키워가면 됩니다. 🚀

---

### ➡️ 다음: [3부 · 나만의 프로젝트 처음부터 만들기](실습가이드-3부.md)
원서 앱을 복사해서 돌려봤으니, 이번엔 **내가 원하는 걸 백지에서** 클로드와 함께 만들어 봐요.

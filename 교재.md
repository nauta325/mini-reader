# 📘 미니 원서 앱 만들기 — 자습 교재 (혼자서도 처음부터 끝까지)

> 이 교재는 **영상 없이 혼자** 따라 할 수 있게 만든 자세한 설명서예요.
> 이 저장소를 **Fork(복사)** 했다면 이 교재는 **여러분 저장소 안에 영원히** 있습니다. 언제든 다시 보세요.
>
> **읽는 법**: 위에서부터 순서대로. 각 단계는 `🔎 왜?` → `🖱 할 일` → `👀 이렇게 보여요` → `✅ 확인` → `🆘 막히면` 순서예요.
> 급하면 요약본만: **[2부 실습가이드](실습가이드.md)** · **[3부 실습가이드](실습가이드-3부.md)**

---

## 0. 시작 전에 — 이게 다 뭐예요? (개념 5분)

우리는 **코드를 몰라도** 웹앱을 만들고 인터넷에 올립니다. 도구 4개를 씁니다:

| 도구 | 한 줄로 | 비유 |
|---|---|---|
| **GitHub** | 코드가 사는 창고 | 앱의 '원본 파일 보관함' |
| **Supabase** | 데이터베이스 | 학생·점수가 저장되는 '데이터 서랍장' |
| **Vercel** | 배포(인터넷에 띄우기) | 창고의 코드를 '실제 웹사이트'로 켜주는 스위치 |
| **Claude Code** | AI 코딩 도우미 | 말로 시키면 코드를 고쳐주는 '개발자' |

**흐름은 늘 같아요**: `GitHub(코드)` → `Vercel(배포)` → 인터넷 주소로 열림. 데이터가 필요하면 `Supabase` 연결. 바꾸고 싶으면 `Claude Code`에게 말하기.

> 💡 **바이브 코딩** = 코드를 직접 안 짜고, **하고 싶은 걸 말로** 설명해서 AI가 만들게 하는 방식. 이 교재의 핵심이에요.

**미리 준비**: 위 4개 계정. (Vercel·Supabase는 "Continue with GitHub"으로 가입하면 편해요.)

---

# 2부 · 내 원서 앱을 복사해서 인터넷에 올리기

> 목표: **1시간 안에** 내 원서 학습 앱이 인터넷 주소로 열리게 하기.

## 2-1. 코드 복사하기 (Fork)

🔎 **왜?** 강사의 코드를 내 것으로 통째 복사해야, 내가 마음대로 바꾸고 배포할 수 있어요.

🖱 **할 일**
1. 강사 저장소(`github.com/nauta325/mini-reader`)를 엽니다.
2. 오른쪽 위 **`Fork`** → **`Create fork`**.

👀 **이렇게 보여요**: 잠깐 로딩 후, 주소가 `github.com/(내아이디)/mini-reader`로 바뀝니다.

✅ **확인**: 주소에 **내 아이디**가 들어가 있으면 성공.

🆘 **막히면**: `Fork` 버튼이 안 보이면 로그인이 안 된 거예요. 우측 위에서 로그인 먼저.

> ⚠️ **GitHub 첫 화면 당황 방지** — 로그인하면 "Home · Ask anything · Create project…" 같은 화면이 떠요. **여기선 아무것도 안 눌러도 돼요.** 강사 저장소 주소로 **바로 이동해서 Fork**만 하세요. (홈의 `Create repository`·`Create project`는 3부에서 써요.)

---

## 2-2. 데이터베이스 만들기 (Supabase)

🔎 **왜?** 학생·책·점수 같은 정보를 저장할 '서랍장'이 필요해요. 그게 데이터베이스고, Supabase가 무료로 줍니다.

🖱 **할 일 (A) 프로젝트 생성**
1. [supabase.com](https://supabase.com) 로그인 → **`New project`**.
2. 이름 아무거나(예: `my-reader`), **Region = `Northeast Asia (Seoul)`**, DB 비밀번호 정해서 **메모장에 적어두기**.
3. **`Create new project`** → 1~2분 기다림(초록불 될 때까지).

> ⚠️ **꼭 '새' 프로젝트로.** 예전에 쓰던 프로젝트에 섞으면 나중에 "어느 게 내 앱 DB지?"로 헷갈립니다. (실제로 제일 많이 하는 실수예요.)

🖱 **할 일 (B) 표 만들기**
1. 왼쪽 **`SQL Editor`** → **`New query`**.
2. 내 저장소에서 **`sql/mini-setup.sql`** 파일을 엽니다 → **`Raw`** 버튼 → 전체 복사(Ctrl/Cmd+A, Ctrl/Cmd+C).
3. SQL Editor에 붙여넣기 → 오른쪽 **`Run`** ▶.

👀 **이렇게 보여요**: 아래에 **`Success. No rows returned`** 비슷한 초록 메시지.

✅ **확인**: 왼쪽 **`Table Editor`**에 `mini_books`, `mini_students` … 표들이 생겼으면 성공.

🖱 **할 일 (C) 데모 데이터 (선택이지만 추천)**
- 같은 방식으로 **`sql/mini-seed-demo.sql`**도 Run → 학생 6명·책 4권이 들어가서, 배포하면 화면이 바로 채워져 보여요.

🖱 **할 일 (D) 주소·키 복사** — 다음 단계에서 씁니다
1. 왼쪽 **`Settings`(⚙️) → `API`**.
2. **`Project URL`** 복사 → 메모 (예: `https://abcd1234.supabase.co`).
3. **`service_role`** 키 복사 → 메모.

> ⚠️ **키 두 종류 주의!** `anon`(공개용) 말고 반드시 **`service_role`(secret, 비밀용)** 을 복사하세요. 이걸 잘못 넣으면 저장·로그인이 안 돼요. 그리고 이 키는 **비밀번호처럼** 취급 — 누구에게도 보여주지 말고, 코드/화면에 넣지 마세요.

🆘 **막히면**: `column ... does not exist` 오류가 나중에 나면, `mini-setup.sql`을 **끝까지 다** 실행하지 않은 거예요. 처음부터 전체 다시 Run.

---

## 2-3. 인터넷에 올리기 (Vercel) — ⭐제일 중요한 함정⭐

🔎 **왜?** GitHub의 코드는 아직 '창고 속'일 뿐. Vercel이 이걸 **실제 웹사이트로 켜줍니다.**

> ⭐ **핵심 함정 예방**: 환경변수(키·주소)는 **"Deploy 버튼 누르기 전에"** 넣으세요. 배포 후에 넣으면 **다시 배포**해야 반영돼서, "왜 안 바뀌지?"로 한참 헤맵니다. (강사도 이걸로 오래 헤맸어요.)

🖱 **할 일**
1. [vercel.com](https://vercel.com) 로그인 → **`Add New…` → `Project`**.
2. 목록에서 내 **`mini-reader`** 옆 **`Import`**.
3. **아직 Deploy 누르지 말고**, 아래 **`Environment Variables`**를 펼쳐 2개 입력:

   | Name | Value |
   |---|---|
   | `SUPABASE_URL` | 2-2(D)의 **Project URL** |
   | `SUPABASE_SERVICE_KEY` | 2-2(D)의 **service_role 키** |

   - **AI 자동 문제생성**을 쓰려면 `ANTHROPIC_API_KEY`도 추가하세요. 이미 키가 있으면 그 값을, 없으면 **바로 아래 「🔑 Anthropic API 키 발급받기」** 를 먼저 하고 오세요.
4. 이제 **`Deploy`** → 1~2분 기다림.

👀 **이렇게 보여요**: **`Congratulations!` 🎉** 화면. **`Continue to Dashboard`** → 상단 **`Visit`** 버튼.

✅ **확인**: 다음 단계에서 주소로 접속해 화면이 뜨면 성공.

🆘 **막히면**: 배포는 됐는데 나중에 화면에서 `undefined/rest/v1…` 오류가 나면 → `SUPABASE_URL`을 안 넣었거나 오타. Vercel **Settings → Environment Variables**에서 고치고 **Deployments → ⋯ → Redeploy**.

---

## 🔑 Anthropic API 키 발급받기 (AI 자동생성용)

🔎 **왜?** 지문을 넣으면 **AI가 단어카드·퀴즈를 자동으로 만들어** 줘요. 이 기능만 `ANTHROPIC_API_KEY`가 필요해요.
- 이건 **Claude 구독(월정액)과 별개**예요 — API는 **쓴 만큼** 요금인데, 이 앱은 제일 싼 모델(Haiku)이라 **아주 조금**(몇 달러) 나와요.
- **2차 세미나에서 이미 발급받았다면** 그 키를 그대로 쓰면 돼요. 키를 못 찾으면(한 번만 보임) **새로 하나 더 만들면** 됩니다 (결제수단이 이미 있어 바로 생성돼요).

🖱 **발급 순서 (처음 받는 경우)**
1. **console.anthropic.com** 접속 → 로그인 (Claude 쓰던 계정)
2. **Billing(결제)** → **Add payment method** → 카드 등록
3. **Buy credits(크레딧 충전)** → 예: **$5** (Haiku라 오래 써요)
4. 왼쪽 **API Keys** → **Create Key** → 이름(예: `mini-reader`) → **Create**
5. 나온 **`sk-ant-...`** 키를 **즉시 복사·메모** ⚠️ *이 창을 닫으면 다시 못 봐요*
6. (권장) **Usage limits / Spend limit** 을 예: **$10/월** 로 걸어두면 과금 걱정 없어요

⚠️ **카드가 안 먹을 때 (2차에서 제일 많이 막힌 곳)**
- **크롬 언어를 영어로** 바꾸고 다시 시도하세요 → 크롬 **⋮ → 설정 → 언어 → English를 맨 위로 → 다시 시작**. (한글 설정일 때 카드 등록이 자꾸 실패하는 경우가 있어요.)
- 그래도 안 되면: **시크릿 창**으로 열기 / **다른 카드**(해외결제 되는 카드) / 잠시 후 재시도.

🖱 **넣는 곳**: Vercel → 프로젝트 → **Settings → Environment Variables** → **`ANTHROPIC_API_KEY`** = `sk-ant-...` → 저장 → **Redeploy**.

✅ **확인**: 원서 관리 → 지문 붙여넣고 **🤖 AI로 생성** → 퀴즈가 만들어지면 성공.

🆘 **막히면**: `ANTHROPIC_API_KEY 미설정` 오류 = 키가 없거나 Redeploy 안 함 → 키 넣고 **Redeploy**. `credit balance is too low` = 크레딧 충전 필요(3번).

---

## 2-4. 내 앱 접속하기

🔎 **왜?** Vercel이 준 주소로 들어가야 내 앱이 보여요.

🖱 **할 일**
- 내 주소는 `https://(내프로젝트).vercel.app` 예요.
- ⚠️ 이 앱의 화면들은 **`/mini/`** 폴더 아래에 있어요. 주소 끝에 **`/mini/`** 를 붙여 여세요:
  ```
  https://(내프로젝트).vercel.app/mini/
  ```
  (그냥 루트로 들어가도 자동으로 `/mini/`로 넘어가게 해뒀어요.)

👀 **이렇게 보여요**: **빨간 책 로고 + "미니 원서"** 홈 화면. 이 홈은 **학생용**이에요 — 가운데 큰 **학생 시작하기** 버튼, 맨 아래 작은 **🔑 선생님용 관리 페이지** 링크.

✅ **확인**
- 학생 로그인 테스트: 학생 시작하기 → 아이디 `seojun` / 이름 `김서준` (데모 데이터를 넣었다면).
- 선생님 화면: 맨 아래 **🔑 선생님용 관리 페이지** → **학생 관리 / 원서 배정·결과 / 원서 관리**. (데모를 넣었다면 원서 배정·결과에 학생 6명이 보임.)

🆘 **막히면**: 루트에서 **404**가 뜨면 주소 끝에 `/mini/`를 붙이세요.

> ### 🔒 왜 학생 화면과 선생님 화면이 나뉘어 있나요? (중요 개념)
> **학생이 관리 화면(성적·다른 학생 정보)을 보면 안 되니까요.** 그래서 홈은 학생용만 보이고, 관리는 **선생님용 페이지(`/mini/admin.html`)** 로 따로 들어갑니다.
> - 처음엔 비밀번호가 없어 누구나 들어갈 수 있어요 → 선생님용 페이지에서 **🔐 관리자 비밀번호 설정**을 한 번 해두세요.
> - 그 뒤로는 학생이 주소를 알아도 관리 페이지를 열면 **비밀번호 창이 떠서 막혀요.**
> - 🔑 **핵심**: 이 비밀번호는 앱 안에서 정하고 **서버에만 저장**돼요 — 코드·주소 어디에도 안 보입니다. (2-3에서 배운 "**비밀값은 코드에 안 넣는다**"와 같은 원칙!)

---

## 2-4½. 🧪 다 같이 해보는 공통 테스트 (앱이 살아있는지 확인)

🔎 **왜?** **2부는 모두 같은 앱**이에요. 그래서 **똑같은 순서로** 테스트하면, "내 것도 잘 되는지"를 확실히 확인할 수 있어요. (3부는 각자 다른 앱이니 각자 확인해요.)

> 데모 데이터(`sql/mini-seed-demo.sql`)를 넣었다고 가정합니다. 아래 **8개를 순서대로** 해보세요.

**👦 학생으로** — 홈에서 **학생 시작하기**
1. 아이디 `seojun` / 이름 `김서준` 으로 **로그인**
2. 배정된 책 하나 열기 → **단어 학습** 카드 몇 장 넘겨보기
3. **소리내어 읽고 녹음** → 🎤 시작 → 한두 문장 읽고 → ⏹ 멈추기 → **"✅ 녹음 저장됐어요"** 뜨는지
4. **원서퀴즈 1** 풀고 제출 → **점수 화면**이 뜨는지

**🔑 선생님으로** — 홈 맨 아래 **🔑 선생님용 관리 페이지**
5. (처음이면) **관리자 비밀번호 설정**을 한 번
6. **원서 배정·결과 → 결과 대시보드** 열기
7. 방금 김서준이 푼 **퀴즈 점수**가 보이는지 · **▶ 녹음 재생**이 되는지
8. **월별 추이 그래프**와 **학생별 현황 표**가 보이는지

✅ **확인**: 8개 다 되면 앱이 **완전히 살아있는 것**이에요. 🎉
🆘 **안 되는 항목별**
- **녹음 저장 실패** → Supabase에 `recordings` 버킷 없음 → `sql/mini-setup.sql` **끝까지** 다시 Run
- **데이터가 안 보임** → `SUPABASE_URL` 오타 / SQL 미실행 → 환경변수·SQL 확인 후 Redeploy
- **AI 문제 생성 오류** → `ANTHROPIC_API_KEY` 미설정 (선택 기능, 이 테스트엔 없어도 됨)

---

## 2-4¾. 📋 이 앱으로 할 수 있는 것 + ➕ 직접 추가해볼 것

🔎 **왜?** 뭘 만든 건지 알아야, "여기서 더 뭘 해볼까"가 보여요.

**선생님이 할 수 있는 것**
- 반·학생 등록 (하나씩 · **붙여넣기 일괄** · **노션에서 가져오기**)
- 원서(책) 등록 — 챕터 · **단어카드** · **원서퀴즈 1·2**를 **AI로 자동 생성**
- **유튜브 스크립트**를 지문으로 자동 가져오기
- 학생별 책 **배정** (반 전체·전체 챕터를 한 번에)
- **결과 대시보드**: 점수 분포 · **월별 추이** · **학생별 현황 표** · **녹음 재생**
- 학생 **리포트** 인쇄/PDF · **워크시트(시험지)** 만들어 인쇄
- **선생님/학생 화면 분리** + 관리자 비밀번호

**학생이 할 수 있는 것**
- 내게 배정된 책 보기 · **책 읽어주기 영상**
- **단어 플래시카드** 학습 + 단어퀴즈
- **소리내어 읽고 녹음**
- **원서퀴즈** 풀고 통과 확인

**➕ 직접 추가해볼 것 (아이디어)** — 클로드에게 **말만** 하면 돼요
- "학생이 퀴즈 통과하면 **포인트·스티커**를 쌓아줘"
- "학생 화면에 **오늘의 목표(예: 책 1권 읽기)**를 보여줘"
- "많이 읽은 학생 **주간 랭킹** 화면을 만들어줘"
- "틀린 단어만 모아 **복습 퀴즈**를 만들어줘"
- "리포트를 **학부모에게 보내기 좋게** 예쁘게 다듬어줘"

> ⚠️ **하나씩!** 만들고 → (위 공통 테스트처럼) **되는지 확인** → 다음. 이게 안전한 리듬이에요.

---

## 2-5. 말로 바꿔보기 (첫 바이브 코딩)

🔎 **왜?** 이게 오늘의 하이라이트. **코드를 몰라도 말로** 앱을 바꿉니다.

### 🖱 A. Claude Code 열고 내 저장소 연결 (크롬 브라우저 — 설치 X)
1. 크롬에서 **claude.ai** 로그인 → **`Code`** 열기 (주소: **code.claude.com**)
2. **새 세션(New)** 시작
3. (처음 한 번만) **GitHub 연결·승인** — 내 GitHub 계정 접근을 허용
4. 작업할 저장소로 **`(내아이디)/mini-reader`** 선택 → 연결
5. 이제 채팅창에 **한국어로 말하면** 그 저장소를 고쳐줘요 (터미널 필요 없음)

### 🎨 B. 이제 내 학원·취향대로 꾸미기 — 이렇게 말해보세요 (하나씩!)

**디자인**
- "대표 색을 우리 학원 색(예: 분홍/민트)으로 바꿔줘"
- "홈 화면 제목을 '○○영어 원서방'으로 바꿔줘"
- "글자를 더 크고 동글동글 귀엽게 해줘"
- "학생 화면에 우리 학원 로고(이모지 📚🌱)를 넣어줘"

**학원 정보**
- "리포트·워크시트에 나오는 학원 이름을 '○○영어'로 바꿔줘"
- "워크시트 맨 위에 우리 학원 이름·전화번호를 넣어줘"

**학습 설정**
- "퀴즈 통과 점수를 80점으로 바꿔줘"
- "레벨 목록을 우리 학원 단계(예: 씨앗·새싹·나무)로 바꿔줘"
- "시리즈 목록을 우리가 쓰는 책들로 바꿔줘"
- "단어 개수 기본값을 10개로 해줘"

**화면 문구·분위기**
- "학생이 퀴즈 통과하면 '참 잘했어요!'를 큰 글자로 축하해줘"
- "로그인 화면 안내 문구를 더 친절하게 바꿔줘"

**화면 구성 (나누기·합치기)** — 화면 짜임새도 말로 바꿔요
- "원서 배정·결과를 **탭으로 나눠**줘" (배정 / 대시보드)
- "결과 대시보드를 **기본 탭**으로 해줘"
- "학생 화면도 **책 읽기 / 내 기록**으로 나눠줘"
- "탭 이름을 우리 학원 말투로 바꿔줘"

**한 걸음 더 (기능)**
- "학생 화면에 오늘 읽은 책 수를 보여줘"
- "리포트에 **월별 추이 그래프**를 넣어줘" · "학생별 현황을 **표**로 보여줘"
- "리포트에 도장(스탬프) 느낌을 넣어줘"

> ⚠️ **한 번에 하나씩!** 여러 개를 한꺼번에 시키면 꼬여요. **하나 바꾸고 → 확인 → 다음** 순서로.
> 💡 "이렇게 **나눠줘** / 저렇게 **합쳐줘**"처럼 **화면 짜임새도 말로** 바꿉니다. 정답이 없으니 사람마다 다르게 나와도 OK.

바꿀 때마다: 클로드가 고쳐서 저장(push) → **Vercel 자동 배포** → 잠시 후 **강력 새로고침**으로 확인.

👀 **이렇게 보여요**: 새로고침하면 색·이름·문구 등이 **내 학원 것으로** 바뀌어 있음.

✅ **확인**: 바뀐 게 보이면, 여러분은 방금 **바이브 코딩**을 한 거예요. 🎉

🆘 **막히면**: 바뀐 게 안 보이면 → ① Vercel에서 배포가 끝났는지 ② **강력 새로고침**(Ctrl/Cmd+Shift+R) ③ **보고 있는 주소가 내 프로젝트 주소가 맞는지**(다른 사이트 보고 있으면 당연히 안 바뀜 — 흔한 실수!).

> ### 🔁 코드 고친 뒤 "반영" 확인 습관 (꼭 기억!)
> 클로드가 코드를 고쳐도 **인터넷 사이트에 자동으로 바로 뜨는 건 아닐 수 있어요.** 매번 이 순서로:
> 1. **Vercel → Deployments** 에 새 배포가 도는지 본다 (자동으로 돌면 그대로 OK)
> 2. 안 돌면 맨 위 배포 **⋯ → Redeploy** ← **"반영 확정 버튼"**
> 3. 완료되면 화면에서 **강력 새로고침**(Ctrl/Cmd+Shift+R)
> 4. 그래도 그대로면 → **지금 보는 주소가 내 프로젝트 주소가 맞는지** 확인
>
> 💡 GitHub Pages(정적 사이트)는 늘 자동 배포지만, **Vercel은 "누가 올렸느냐"에 따라 자동이 안 걸릴 때가 있어요.** 그래서 **Redeploy를 습관**으로 두면 항상 안전합니다. (원한다면 자동 배포 장치를 걸 수도 있어요 — 저장소의 `.github/workflows/deploy.yml` + Vercel Deploy Hook 참고.)

---

# 3부 · 나만의 프로젝트 처음부터 만들기 (백지에서)

> 목표: 복사가 아니라, **내가 원하는 걸** 클로드와 함께 만들기. 도구는 2부와 똑같이 씁니다.

## 3-0. 마음가짐 2가지 (제일 중요)
1. **가장 작은 버전 먼저(MVP).** 한 화면·한 기능. "오늘은 이것 하나만 되면 성공."
2. **완벽하게 X, 일단 되게 O.** 되고 나서 말로 키웁니다.

> 초보 실패 1위 = 처음부터 너무 크게. **기능 1개**로 시작하세요.

## 3-1. 기획 (종이/메모장 5분)

🔎 **왜?** 클로드에게 줄 '설계도'예요. 이게 있어야 클로드가 헤매지 않아요.

🖱 **할 일** — 빈칸 채우기
```
· 앱 이름:
· 한 줄 설명: (누가 / 무엇을 / 하는 앱)
· 쓰는 사람:
· 첫 화면에 뭐가 보이나:
· 저장할 것(데이터):
· 오늘의 첫 목표(이것만 되면 성공):
```

**학원용 아이디어**: 단어 시험지 생성기 · 출석부 · 숙제 체크판 · 독서록 · 상담일지 · 스티커판.

**작성 예시 (단어 시험지 생성기)**
```
· 앱 이름: 단어쪽지
· 설명: 선생님이 단어를 넣으면 시험지로 뽑아주는 앱
· 쓰는 사람: 선생님
· 첫 화면: 단어 입력칸 + [시험지 만들기] 버튼
· 저장할 것: 단어세트(제목 + 단어들)
· 오늘의 첫 목표: 단어 붙여넣고 → 시험지 화면 나오기 (저장은 나중에)
```

## 3-2. 새 GitHub 저장소 만들기
🖱 github.com → 우측 위 **`+` → `New repository`** → 이름(예: `word-quiz`) → **Public** → **☑ Add a README** → **`Create repository`**.
✅ 빈 저장소 하나 생김.
> ⚠️ **"저장소(repository)"를 만드세요.** 홈에 크게 보이는 **`Create project` / `Create your first code project`는 다른 것**(코파일럿용)이에요.

## 3-3. Claude Code 새 채팅 + 저장소 연결

🔎 **왜?** 이제 클로드가 백지에서 코드를 만들어줍니다.

🖱 **할 일**: 크롬에서 **code.claude.com → 새 세션** → 이번엔 **방금 만든 새 저장소**를 선택해 연결 → **첫 지시**로 이렇게(기획서 붙여넣고):
```
아래 기획으로 웹앱을 만들어줘.
[3-1 기획서 붙여넣기]

구조는 mini-reader 처럼 아주 단순하게:
- 화면은 public 폴더에 HTML 파일로 (빌드·프레임워크 없이)
- 서버가 필요하면 api 파일 하나로 (순수 fetch)
- 데이터는 Supabase에 저장 (필요하면 만들 SQL도 같이 줘)
- API 키·비밀값은 절대 HTML에 넣지 말고 환경변수로
일단 '오늘의 첫 목표' 기능 하나만 되게 가장 작게 만들어줘.
```

👀 클로드가 코드 + (필요하면) Supabase용 SQL을 만들어 저장해줍니다.

🆘 **막히면**: 그냥 한국어로 말하세요 — "에러가 나 [메시지]", "이 버튼이 안 눌려".

## 3-4. 새 Supabase (데이터 저장이 필요할 때만)
> 데이터를 저장 안 하는 앱이면 건너뛰기.
🖱 supabase.com → New project(Seoul) → 클로드가 준 **SQL을 SQL Editor에 Run** → **Project URL + service_role 키** 복사.

## 3-5. 새 Vercel (배포)
> ⭐ 2부와 똑같이 **환경변수를 Deploy 전에** 넣기.
🖱 vercel.com → Add New → Project → 내 새 저장소 Import → (Supabase 쓰면) 환경변수 2개 → **Deploy** → **Visit**.
- 화면 주소가 `/` 인지 `/어떤경로/` 인지 헷갈리면 클로드에게 "메인 주소가 뭐야?"라고 물어보세요.

## 3-6. 말해서 키우기 (반복)
`말 → 저장 → 자동배포 → 새로고침`을 반복. **한 번에 하나씩.**
예: "저장 기능 추가해줘" → "인쇄 버튼 만들어줘" → "색 바꿔줘" → "저학년용으로 더 예쁘게".

## 🎯 3부 성공 기준
- [ ] 내 앱이 인터넷 주소로 열린다
- [ ] 첫 목표 기능 하나가 동작한다
- [ ] 클로드에게 한 가지를 말해서 바꿔봤다

---

# 🧰 자주 막히는 곳 (모아보기)

| 증상 | 원인 · 해결 |
|---|---|
| 루트에서 **404** | 앱은 `/mini/`에 있음 → 주소 끝에 `/mini/` |
| `undefined/rest/v1…` | `SUPABASE_URL` 비었거나 오타 → 고치고 **Redeploy** |
| `column … does not exist` | 세팅 SQL을 끝까지 실행 안 함 → 전체 다시 Run |
| `PGRST204 … schema cache` | 표는 맞는데 캐시가 옛것 → Supabase **Settings→General→Restart project** |
| 저장·로그인이 안 됨 | 키가 `anon`일 수 있음 → **`service_role`(secret)** 으로 교체 후 Redeploy |
| **코드 고쳤는데 그대로** | ① **Redeploy** 했나 ② **강력 새로고침**(Ctrl/Cmd+Shift+R) ③ 보는 주소가 **내 프로젝트** 맞나 |
| 학생 수·데이터가 이상 | 앱이 보는 DB = `SUPABASE_URL`이 가리키는 그 Supabase. 프로젝트 여러 개면 **어느 걸 가리키는지** 확인 |

---

# 📖 용어 사전 (모르는 말이 나오면)

| 말 | 뜻 |
|---|---|
| **저장소(Repository/Repo)** | 코드 파일들이 모여 있는 프로젝트 폴더(인터넷판) |
| **Fork(포크)** | 남의 저장소를 내 계정으로 복사 |
| **배포(Deploy)** | 코드를 실제 웹사이트로 켜기 |
| **환경변수(Environment Variable)** | 키·주소 같은 값을 코드 밖(서버)에 안전하게 저장하는 칸 |
| **SQL** | 데이터베이스에게 "표 만들어/데이터 넣어"라고 시키는 명령어 |
| **service_role 키** | 데이터베이스를 마음대로 읽고 쓸 수 있는 **비밀** 열쇠 (서버에서만 사용) |
| **PWA / 홈 화면에 추가** | 웹앱을 폰 홈에 앱 아이콘처럼 두는 기능 |
| **강력 새로고침** | 브라우저가 캐시(옛 화면)를 버리고 새로 불러오기 (Ctrl/Cmd+Shift+R) |

---

**끝까지 오셨어요! 👏** 이제 여러분은 코드를 몰라도 **앱을 만들고·올리고·말로 바꾸는** 사람이 됐어요.
막히면 이 교재의 "자주 막히는 곳"을 먼저 보고, 그래도 안 되면 **클로드에게 증상을 그대로 말하세요.**

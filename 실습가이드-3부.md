# 🚀 3부 · 나만의 프로젝트 처음부터 만들기 (백지에서)

> 2부에서 **내 원서 앱을 복사→배포→수정**해봤죠. 이제는 복사가 아니라,
> **내가 원하는 걸 클로드와 함께 처음부터** 만듭니다.
> 도구(깃허브·Supabase·Vercel·클로드코드)는 2부에서 이미 익혔으니, **똑같은 흐름**이에요.

---

## 0단계 · 마음가짐 2가지 (제일 중요)

1. **가장 작은 버전 먼저 (MVP).** 한 화면·한 기능이면 충분. "오늘은 이것 하나만 되면 성공."
2. **완벽하게 X, 일단 되게 O.** 되고 나서 **말로 조금씩** 키웁니다. (그게 바이브 코딩)

> 초보가 실패하는 이유 1위 = **처음부터 너무 크게** 잡아서. 기능 1개로 시작하세요.

---

## 1단계 · 기획 (종이/메모장에 5분)

아래 빈칸을 채우면 그게 클로드에게 줄 "설계도"예요.

```
[ 기획서 ]
· 앱 이름:
· 한 줄 설명: (누가 / 무엇을 / 하는 앱)
· 쓰는 사람:
· 첫 화면에 뭐가 보이나:
· 저장할 것(데이터):
· 오늘의 첫 목표(이것만 되면 성공):
```

**학원에서 쓸 만한 아이디어 예시**
- 단어 시험지 생성기 · 출석부 · 숙제 체크판 · 독서록 · 상담일지 · 간단 레벨테스트 · 스티커/포인트판

**작성 예시 (단어 시험지 생성기)**
```
· 앱 이름: 단어쪽지
· 한 줄 설명: 선생님이 단어 목록을 넣으면 시험지로 뽑아주는 앱
· 쓰는 사람: 선생님
· 첫 화면: 단어 입력칸 + [시험지 만들기] 버튼
· 저장할 것: 단어세트(제목 + 단어들)
· 오늘의 첫 목표: 단어 붙여넣고 → 시험지 화면이 나오기 (저장은 나중에)
```

---

## 2단계 · 새 GitHub 저장소 만들기

1. github.com → 오른쪽 위 **`+` → `New repository`**
2. 이름 (예: `word-quiz`) → **Public** → **☑ Add a README file** → **`Create repository`**
3. → 빈 저장소 하나 완성 ✅

---

## 3단계 · Claude Code 새 채팅 + 저장소 연결

1. 크롬에서 **code.claude.com** → **새 세션** → 방금 만든 저장소 **선택해 연결** (처음이면 GitHub 승인)
2. **첫 지시**로 기획서를 붙여넣고, 이렇게 말하세요 (mini-reader와 같은 단순 구조 요청):

```
아래 기획으로 웹앱을 만들어줘.
[여기에 1단계 기획서 붙여넣기]

구조는 mini-reader 처럼 아주 단순하게:
- 화면은 public 폴더에 HTML 파일로 (빌드·프레임워크 없이)
- 서버가 필요하면 api 파일 하나로 (순수 fetch)
- 데이터는 Supabase에 저장 (필요하면 만들 SQL도 같이 줘)
- API 키·비밀값은 절대 HTML에 넣지 말고 환경변수로

일단 '오늘의 첫 목표' 기능 하나만 되게 가장 작게 만들어줘.
```

3. 클로드가 **코드 + (필요하면) Supabase용 SQL**을 만들어 저장(push)해줍니다.

> 💡 막히면 그대로 말하세요: "에러가 나 [메시지 붙여넣기]", "이 버튼이 안 눌려" — 클로드가 고쳐요.

---

## 4단계 · 새 Supabase 프로젝트 (데이터 저장이 필요할 때만)

> 데이터를 저장하지 않는 앱(예: 그냥 시험지 뽑기)이면 **이 단계 건너뛰어도 돼요.**

1. supabase.com → **New project** (Region **Seoul**, 비밀번호 메모)
2. 클로드가 준 **SQL을 SQL Editor에 붙여넣고 Run**
3. **Project URL** → Settings → **Data API** · **service_role** 키 → Settings → **API Keys** → Reveal → Copy

---

## 5단계 · 새 Vercel 프로젝트 (배포)

> ⭐ 2부와 똑같은 함정 예방: **환경변수를 "Deploy 누르기 전에" 넣기**

1. vercel.com → **Add New → Project** → 내 새 저장소 **Import**
2. **Deploy 전에** Environment Variables 추가 (Supabase 쓰는 경우):
   - `SUPABASE_URL` = 4단계 Project URL
   - `SUPABASE_SERVICE_KEY` = 4단계 service_role 키
   - (AI 기능 넣었으면) `ANTHROPIC_API_KEY`
3. **Deploy** → 완료되면 **Visit** 으로 내 앱 열기 ✅
   - 클로드가 화면을 어디에 뒀는지에 따라 주소가 `/` 또는 `/어떤경로/` 일 수 있어요. 안 열리면 클로드에게 "메인 주소가 뭐야?"라고 물어보세요.

---

## 6단계 · 말해서 키우기 (반복 루프)

이제부터는 **말 → 저장 → 자동 배포 → 새로고침** 의 반복:

| 이렇게 말하면 | |
|---|---|
| "저장 기능도 추가해줘" | 데이터가 남게 |
| "인쇄 버튼 만들어줘" | PDF로 출력 |
| "색을 우리 학원 색으로" | 디자인 |
| "이 화면을 더 예쁘게, 저학년용으로" | 스타일 |

> **한 번에 하나씩.** 크게 여러 개를 한 번에 시키면 꼬여요.

---

## ❓ 자주 막히는 곳 (2부와 동일)

| 증상 | 해결 |
|---|---|
| 코드 고쳤는데 사이트 그대로 | ① Vercel **Redeploy** ② **강력 새로고침**(Ctrl/Cmd+Shift+R) ③ 보는 주소가 **내 프로젝트** 맞는지 |
| `undefined/rest/v1…` | `SUPABASE_URL` 오타/누락 → 고치고 Redeploy |
| `column … does not exist` / `PGRST204` | 클로드가 준 SQL을 **끝까지** 실행 안 함 → 다시 Run → 안 되면 Supabase **Restart project** |
| 저장이 안 됨 | 키가 `anon`일 수 있음 → **`service_role`(secret)** 으로 교체 |
| 뭘 어떻게 말할지 모르겠음 | 그냥 **하고 싶은 걸 한국어로** 말하세요. "○○가 안 돼", "○○ 추가해줘" 면 충분 |

---

## 🎯 오늘의 성공 기준
- [ ] 내 앱이 **인터넷 주소**로 열린다
- [ ] **첫 목표 기능 하나**가 동작한다
- [ ] 클로드에게 **한 가지를 말해서** 바꿔봤다

이 셋이면 대성공이에요. 나머지는 집에서 말로 키우면 됩니다. 🚀
